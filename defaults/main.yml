---
# defaults file for mariadb

mariadb_packages:
  - mariadb-client
  - mariadb-server

mariadb_service: mariadb

mariadb_base_path: /usr

mariadb_data_path: /var/lib/mysql

mariadb_config_path: /etc/mysql

mariadb_user: mysql
mariadb_group: mysql

# mariadb_config: {}
